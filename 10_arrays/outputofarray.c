#include <stdio.h>
int main()
{
    int x[5];

    for (int i = 0; i <=4; i++)
    {   scanf("%d",&x[i]);
    }
    for (int i = 0; i <=4; i++)
    {   printf("%d ",x[i]);
    }

    return 0;
}